//use for export component if there are to many components file then, import all of them in one palce and then export them , 
// and when need of them then we access them only by importing one file index.js

import InputBox from "./InputBox";

export {InputBox};
